version: "3.6"
services:
  home:
    container_name: home
    restart: always
    privileged: true
    build:
      context: ./home
      args:
        branchname: grow_tent
        cfgfilename: grow_tent
    logging:
      options:
        max-size: "1m"
        max-file: "3"
